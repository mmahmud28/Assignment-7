import Image from 'next/image';
import React from 'react';

const MainView = ({ data }) => {
    return (
        <div className='border border-amber-100 py-20'>
            
        </div>
    );
};

export default MainView;